---
title: Example Service Three
excerpt: A third example so the services index shows a full row of cards.
icon: tabler:sparkles
order: 3
---

**Example service — replace or delete this file.**

Add as many services as the client offers. Set `order` to control where each one
appears on the index, `icon` for the card (any [Tabler icon](https://tabler.io/icons)),
and `draft: true` to hide one without deleting it.
